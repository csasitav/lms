@Library("lms-jhc-slibs") _
pipeline {
    agent any

    stages {
        stage('Name calling') {
            steps {
                welcome("Asitav")
                welcome("Ravindra")
                welcome("Amitav")
            }
        }
    }
}
